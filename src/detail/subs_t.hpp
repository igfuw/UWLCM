#pragma once

enum class subs_t { local=0, mean, none }; // local - separate in each column
                                           // mean - subsidence of the horizontal mean
                                           // none - no subsidence
